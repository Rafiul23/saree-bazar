

const SectionTitle = ({title}) => {
    return (
        <div>
            <h2 className='text-center lg:text-5xl md:text-3xl text-xl text-[#fa8072] font-bold my-5'>{title}</h2>
        </div>
    );
};

export default SectionTitle;